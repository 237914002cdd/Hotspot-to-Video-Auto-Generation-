# 本地创作者工作区验收记录

验收日期：2026-09-23（Asia/Hong_Kong）。源码目录：`D:\codex\hotspot-video-platform`，分支：`codex/creator-platform`。

## 已完成的验收

| 范围 | 实测结果 |
|---|---|
| 构建 | `npm run build` 成功；CSS、字体和图标本地提供，无需第三方 CDN |
| 自动测试 | `npm test`：37 通过，0 失败，0 跳过 |
| 数据和 API | 12 组集成测试，覆盖持久化、备份、冲突、中文项目唯一性、封面实际排版、非法输入、损坏数据、路径链接、来源校验、SSE 与归档 |
| 异步编辑状态 | 21 项延迟响应测试；保存期间切换项目、同项目重开、乱序打开、渲染/预览/归档后续操作不污染新编辑器；独立复核通过 |
| 渲染 | 4 组测试，包括真实 MP4、ffprobe、取消、重启恢复、失败保留旧版本、文字无损分段、三画幅极限文本排版 |
| 浏览器流程 | `npm run test:ui` 成功；01:13:58 结束，页面脚本错误 0，请求错误 0 |
| 实际热点 | 01:13:25 三源均成功：GitHub 10、Hacker News 6、头条 10；总计 26 条，保留来源与状态 |
| 依赖 | `npm audit`：已知漏洞 0；不是对全部安全问题的保证 |

浏览器验收使用隔离临时工作区，实际完成新建选题、编辑方案、生成分镜、保存、渲染、刷新恢复任务、播放、下载、修改封面、归档、关联人工发布、指标及笔记录入、备份下载、主题切换和移动端总览布局。渲染样本为 4 秒、720×1280 的 H.264 MP4。测试中的平台指标是隔离验收数据，不混入日常工作区。

原始证据保存在被 Git 忽略的 `看板/data/qa/`：`ui-evidence.json`、`render-evidence.json`、`browser-downloaded.mp4`、`verified-render.mp4`、`studio-desktop.png`、`overview-desktop.png`、`overview-dark.png`、`overview-mobile.png`、`review-desktop.png`、`render-frame.png`、`cover-long-fields.png`。可重新运行测试生成。

## 用户可见演示

工作区保留明确标注的「本地制作流程 · 演示项目」，项目标识 `20260923-topic-14716609`。真实任务 `0b9b95de-11af-48b5-8d8a-5c5832775804` 已完成，成片为 17 秒、720×1280、24fps、H.264、无音轨，335307 字节。独立 Chromium 已从头播放到 17 秒结尾，媒体错误为空。

本机地址为 http://127.0.0.1:3456 。演示项目及运行数据仅留本机，不加入代码提交。

## 明确未通过或未覆盖的范围

- Codex 内置浏览器在一次通过无标签原生视频控件点击播放时出现页面崩溃；服务和文件未损坏，页面已另开恢复。同一视频在独立 Chromium 完整播放正常。尚未定位内置浏览器崩溃根因，不能声称该环境播放兼容性已通过。
- AI 模型、配音、素材时间线、社交账号自动发布及指标同步未接入，按用户选择留待下一阶段。现有模板与人工记录已明确标注。
- 未进行公网部署、真实多用户、支付、生产负载、长期运行或跨操作系统验收。当前为本机单进程工作区；不宣称多人 SaaS 已完成。
- 系统文件管理器打开动作有实现，但未作为本次可见窗口交互验收项。旧 HTML 的外部动画依赖受到预览限制，需迁移到当前分镜后导出。
- 测试覆盖列出的行为与边界，不能据此承诺所有环境永远零缺陷。
